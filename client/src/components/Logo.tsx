// React import removed (unused)

const Logo = () => {
    return (
        <div className="flex items-center">
            <img
                src="/logo.png"
                alt="SRM Swap Logo"
                className="h-12 w-auto object-contain"
            />
        </div>
    );
};

export default Logo;
